// ─── locationSeoAiService.js ──────────────────────────────────────────────────
// AI-assisted SEO field generation for location pages.
// Stub — full implementation pending.
// ─────────────────────────────────────────────────────────────────────────────

const noop = async () => null;

export const fillEmptySeoFields       = noop;
export const regenerateAllSeo         = noop;
export const spinAllSeo               = noop;
export const generateSeoTitle         = noop;
export const generateSeoDescription   = noop;
export const generateOgTitle          = noop;
export const generateOgDescription    = noop;
export const generateTwitterTitle     = noop;
export const generateTwitterDescription = noop;
export const generatePrimaryKeyword   = noop;
export const generateSecondaryKeywords = noop;
export const generateSchemaJson       = noop;
export const autoComputeSeoDefaults   = noop;

export const spinField    = noop;
export const shortenField = noop;
